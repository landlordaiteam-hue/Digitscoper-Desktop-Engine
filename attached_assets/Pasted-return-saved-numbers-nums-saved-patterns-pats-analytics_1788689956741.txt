return {"saved_numbers": nums, "saved_patterns": pats, "analytics": {"total_saved_numbers": len(nums), "total_saved_patterns": len(pats)}}@app.get("/admin/db")def admin_db(password: str):if password != ADMIN_PASSWORD:raise HTTPException(status_code=403, detail="Invalid admin password")conn = sqlite3.connect(DB_FILE)cursor = conn.cursor()cursor.execute("SELECT number, lookup_count FROM lookups")rows = cursor.fetchall()conn.close()return {"total_numbers": len(rows), "numbers": [{"number": r[0], "lookup_count": r[1]} for r in rows]}@app.post("/admin/add_user")def admin_add_user(password: str, email: str, user_password: str, is_pro: bool = True):if password != ADMIN_PASSWORD:raise HTTPException(status_code=403, detail="Invalid admin password")conn = sqlite3.connect(DB_FILE)cursor = conn.cursor()cursor.execute("INSERT OR REPLACE INTO users VALUES (?, ?, ?, ?)", (email, hash_password(user_password), 1 if is_pro else 0, datetime.utcnow().isoformat()))conn.commit()conn.close()return {"status": "user_added", "email": email}==============================================================================3. INTERFACE VISUAL LAYER (Embedded Dashboard UI String)==============================================================================INDEX_HTML = """Digitscoper Intelligence Terminalbody { margin: 0; font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; background: radial-gradient(circle at top, #1f2933, #020617); color: #e5e7eb; display: flex; flex-direction: column; min-height: 100vh; }.header { padding: 16px 24px; border-bottom: 1px solid #111827; display: flex; justify-content: space-between; align-items: center; background: rgba(15, 23, 42, 0.9); }.logo { font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: #38bdf8; }.tabs { display: flex; gap: 12px; }.tab { padding: 6px 12px; border-radius: 9999px; border: 1px solid #1f2937; cursor: pointer; font-size: 13px; color: #9ca3af; }.tab.active { background: #0ea5e9; color: #0b1120; border-color: #0ea5e9; }.main { flex: 1; display: flex; padding: 16px 24px; gap: 16px; }.panel { flex: 2; background: rgba(15, 23, 42, 0.85); border-radius: 12px; padding: 16px; border: 1px solid #1f2937; display: flex; flex-direction: column; gap: 12px; }.side { flex: 1; background: rgba(15, 23, 42, 0.85); border-radius: 12px; padding: 16px; border: 1px solid #1f2937; display: flex; flex-direction: column; gap: 12px; }.label { font-size: 12px; text-transform: uppercase; letter-spacing: 0.08em; color: #6b7280; margin-top: 4px; }.input-row { display: flex; gap: 8px; align-items: center; }input, select { padding: 8px 10px; border-radius: 8px; border: 1px solid #374151; background: #020617; color: #e5e7eb; font-size: 13px; }input[type="text"], input[type="email"], input[type="password"] { flex: 1; }button { padding: 8px 12px; border-radius: 8px; border: none; cursor: pointer; font-size: 13px; font-weight: 500; background: #0ea5e9; color: #0b1120; }.btn-secondary { background: #111827; color: #e5e7eb; border: 1px solid #374151; }.section { border-top: 1px solid #111827; padding-top: 10px; margin-top: 10px; }.grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; font-size: 13px; }.item-label { color: #9ca3af; font-size: 11px; text-transform: uppercase; letter-spacing: 0.08em; }.item-value { color: #e5e7eb; font-size: 13px; font-weight: 500; }.status { font-size: 12px; color: #22c55e; min-height: 16px; }.error { color: #ef4444 !important; }.scroll { max-height: 200px; overflow-y: auto; font-size: 12px; }.pill { display: inline-block; padding: 4px 8px; border-radius: 9999px; background: #111827; color: #e5e7eb; font-size: 11px; margin: 2px; border: 1px solid #1f2937; }.pill.yes { background: rgba(34, 197, 94, 0.15); color: #22c55e; border-color: rgba(34, 197, 94, 0.3); }.pill.no { background: rgba(239, 68, 68, 0.15); color: #ef4444; border-color: rgba(239, 68, 68, 0.3); }.footer { padding: 12px 24px; text-align: center; font-size: 11px; color: #4b5563; border-top: 1px solid #111827; background: #020617; }DIGITSCOPER TerminalLookup EnginePro RecordsAdmin HubMain Target SearchScan IntelSuffix Pattern Search408 (San Jose Base)415 (San Francisco)510 (Oakland)650 (Silicon Valley)714 (Orange County)ComputeAnalysis Results CardCarrier BrandLine ClassificationTarget RegionTimezoneSpam Score AssessmentRisk Threshold ProfileBusiness StatusExact Business MatchCross-Web Directory IndexingPublic Record Signal SummaryPro Verification AccessVerify SessionPro Sync Automated History Storage LedgerLogin to view synchronized lookups.Admin Operations AuthorizationInspect System LedgerSystem User Registration CoreRegisterSystem Scan Database FeedLive Session Tracking MetricsAuthenticated IdentityNoneLast Target SourcedNoneLocal File Ledger Nodedigitscoper.db©  DIGITSCOPER Core Engine. Operational Security Secured.let proEmail = null;const host = window.location.origin;// Smart client-side localized area code lookupswindow.addEventListener('load', () => {if (navigator.geolocation) {navigator.geolocation.getCurrentPosition((pos) => {const lat = pos.coords.latitude;const lon = pos.coords.longitude;// Simple routing bounds check for California coordinate bounds mappingif (lat > 36.5 && lat < 38.0 && lon > -122.5 && lon < -121.5) {const selector = document.getElementById('pattern-area-code');selector.value = "408"; // Instantly flag and float native home area code base first}}, () => { console.log("Location access bypassed, using hardware defaults."); });}});function setTab(tab) {['lookup', 'pro', 'admin'].forEach(t => {document.getElementById(tab-\${t}).classList.remove('active');document.getElementById(panel-\${t}).style.display = 'none';});document.getElementById(tab-\${tab}).classList.add('active');document.getElementById(panel-\${tab}).style.display = 'flex';}async function doLookup() {const number = document.getElementById('lookup-number').value.trim();const status = document.getElementById('lookup-status');const resultPanel = document.getElementById('lookup-result');if (!number) return;status.textContent = "Scanning deep network channels...";try {const res = await fetch(host/lookup/{encodeURIComponent(number)});const data = await res.json();status.textContent = "Analysis sequence secure.";resultPanel.style.display = 'block';document.getElementById('carrier-name').textContent = data.carrier.name;document.getElementById('carrier-line').textContent = data.carrier.line_type;document.getElementById('carrier-region').textContent = data.region.city + ", " + data.region.state;document.getElementById('carrier-tz').textContent = data.region.timezone;document.getElementById('spam-score').textContent = data.spam.score;document.getElementById('spam-label').textContent = data.spam.label;document.getElementById('biz-listed').textContent = data.business.listed ? "Yes" : "No";document.getElementById('biz-name').textContent = data.business.name || "No commercial signature found";// Direct label string value processing mapping true/false to readable formatdocument.getElementById('dir-labels-box').innerHTML = <div>Yelp Record: <span class="pill data.directories.yelp ? 'yes':'no'">{data.directories.yelp ? 'Yes':'No'}</span></div> <div>Google Business: <span class="pill \({data.directories.google_business ? 'yes':'no'}">\){data.directories.google_business ? 'Yes':'No'}</span></div>;document.getElementById('pr-labels-box').innerHTML = `Corporate Registrations: ${data.public_records.business_registration ? 'Yes':'No'}Active Court Filings: ${data.public_records.court_filings ? 'Yes':'No'}`;document.getElementById('session-last-lookup').textContent = number;// Fire safe pipeline background updates if a verified user session is activeif(proEmail) {await fetch(${host}/pro/auto_save, {method: 'POST',headers: {'Content-Type': 'application/json'},body: JSON.stringify({email: proEmail, number: number, carrier: data.carrier.name,line_type: data.carrier.line_type, region: data.region.city, business_name: data.business.name || "None"})});proDashboard();}} catch(e) { status.textContent = "Engine exception."; }}async function generateSuffixCombinations() {const suffix = document.getElementById('pattern-suffix').value.trim();const ac = document.getElementById('pattern-area-code').value;const targetDisplay = document.getElementById('pattern-results-box');if(suffix.length !== 4) return alert("Please submit exactly a 4-digit token segment.");targetDisplay.innerHTML = "Computing algorithmic permutations...";// Simulating target scan permutation array processingsetTimeout(() => {let html = "Identified Target Permutations";const variations = ["201", "305", "441", "702", "883"];variations.forEach(v => {const constructedNumber = +1 (${ac}) ${v}-${suffix};html += <div style='padding:6px; background:#020617; border-radius:6px; border:1px solid #1f2937;'> <div style='color:#38bdf8; font-weight:bold; cursor:pointer;' onclick='selectPermutation("${constructedNumber}")'>${constructedNumber}</div> </div>;});html += "";targetDisplay.innerHTML = html;}, 300);}function selectPermutation(num) {document.getElementById('lookup-number').value = num;doLookup();}async function proLogin() {const email = document.getElementById('pro-email').value.trim();const password = document.getElementById('pro-password').value.trim();const status = document.getElementById('pro-status');try {const res = await fetch(${host}/pro/login, {method: 'POST',headers: {'Content-Type': 'application/json'},body: JSON.stringify({email, password})});if(!res.ok) throw new Error("Verification credentials invalid.");proEmail = email;document.getElementById('session-pro-user').textContent = email;status.textContent = "Active session authorized.";proDashboard();} catch(e) { status.textContent = e.message; status.classList.add('error'); }}async function proDashboard() {if(!proEmail) return;const res = await fetch(${host}/pro/dashboard?email=${encodeURIComponent(proEmail)});const data = await res.json();let html = "";data.saved_numbers.forEach(n => {html += <div style='background:#020617; padding:8px; border-radius:6px; border:1px solid #1f2937;'> <div style='color:#38bdf8; font-weight:bold;'>${n.number}</div> <div style='font-size:11px; color:#9ca3af;'>${n.carrier} • ${n.line_type} • ${n.business_name}</div> </div>;});html += "";document.getElementById('pro-dashboard').innerHTML = html;}async function adminLoadDB() {const password = document.getElementById('admin-password').value.trim();const status = document.getElementById('admin-status');try {const res = await fetch(${host}/admin/db?password=${encodeURIComponent(password)});if(!res.ok) throw new Error("Admin authentication blocked.");const data = await res.json();status.textContent = "Secure schema metrics verified.";document.getElementById('admin-db').innerHTML = data.numbers.map(n => <div>• Target: ${n.number} [Scans Logged: ${n.lookup_count}]</div>).join("");} catch(e) { status.textContent = e.message; status.classList.add('error'); }}async function adminAddUser() {const password = document.getElementById('admin-password').value.trim();const email = document.getElementById('admin-new-email').value.trim();const userPassword = document.getElementById('admin-new-password').value.trim();const status = document.getElementById('admin-status');try {const res = await fetch(${host}/admin/add_user?password=${encodeURIComponent(password)}&email=${encodeURIComponent(email)}&user_password=${encodeURIComponent(userPassword)}, {method:'POST'});if(!res.ok) throw new Error("Action denied.");status.textContent = "New profile encrypted and saved.";adminLoadDB();} catch(e) { status.textContent = e.message; status.classList.add('error'); }}"""@app.get("/", response_class=HTMLResponse)def index():return HTMLResponse(INDEX_HTML)def start_backend():uvicorn.run(app, host="127.0.0.1", port=8000, log_level="warning")def start_app():t = threading.Thread(target=start_backend, daemon=True)t.start()webview.create_window("Digitscoper Engine", "127.0.0", width=1100, height=700)webview.start()if