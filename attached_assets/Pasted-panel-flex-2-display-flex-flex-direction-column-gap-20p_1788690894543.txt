panel { flex: 2; display: flex; flex-direction: column; gap: 20px; }.side { flex: 1; display: flex; flex-direction: column; gap: 20px; }.card { background: #0f172a; border: 1px solid #1e293b; border-radius: 12px; padding: 24px; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.1); }.label { font-size: 12px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: #64748b; margin-bottom: 12px; border-bottom: 1px solid #1e293b; padding-bottom: 6px; }.input-row { display: flex; gap: 10px; align-items: center; margin-bottom: 12px; }input, select { flex: 1; padding: 12px; border-radius: 8px; border: 1px solid #334155; background: #020617; color: #f3f4f6; font-size: 14px; outline: none; }input:focus { border-color: #38bdf8; }button { padding: 12px 20px; border-radius: 8px; border: none; cursor: pointer; font-size: 14px; font-weight: 700; background: #38bdf8; color: #020617; }button:hover { background: #0ea5e9; }.btn-secondary { background: #1e2937; color: #f3f4f6; border: 1px solid #334155; }.btn-danger { background: #ef4444; color: #ffffff; }/* Fixed Overlapping Issues Natively via Auto-wrapping Flex Layouts */.grid-block { display: flex; flex-direction: column; gap: 12px; margin-top: 10px; }.data-row { display: flex; justify-content: space-between; align-items: center; padding: 12px; background: #1e293b; border-radius: 8px; border: 1px solid #334155; min-height: 24px; }.row-label { font-size: 11px; text-transform: uppercase; color: #94a3b8; font-weight: bold; }.row-value { font-size: 14px; font-weight: 600; color: #ffffff; text-align: right; word-break: break-word; max-width: 60%; }.badge { font-weight: bold; padding: 4px 8px; border-radius: 4px; font-size: 12px; }.badge-yes { background: rgba(16, 185, 129, 0.2); color: #34d399; }.badge-no { background: rgba(148, 163, 184, 0.2); color: #cbd5e1; }.scroll { max-height: 250px; overflow-y: auto; display: flex; flex-direction: column; gap: 8px; }.footer { padding: 16px; text-align: center; font-size: 12px; color: #475569; border-top: 1px solid #1e293b; background: #0b1120; }Digitscoper TerminalLookupPro WorkspaceAdmin ConsoleTarget Intel Scanning EngineRun ScanIntelligence Profile ResultsCarrier BrandLine FootprintBusiness Registry IdentityRisk AppraisalYelp ListingGoogle BusinessCorporate filings4-Digit Suffix Pattern Finder (True Database Match)408 (San Jose)415 (San Francisco)510 (Oakland)Find Matching NumbersPro Account GatewaySign InAuthorized: Sign OutAuto-Saved Pro Intelligence History LedgerLog in to view synchronized assets.Admin Database Inspection DashboardSync System RecordsActive Monitor StreamIdentity ContextNot signed inLast Target SourcedNonePrivacy ParametersAll transactions log natively to your system's SQLite instance. Network mapping targets the local proxy interface.© 2026 DIGITSCOPER Workspace Technology. All Rights Reserved.let authenticatedUser = null;const host = window.location.origin;function setTab(tab) {document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));document.getElementById(tab-\${tab}).classList.add('active');// Manage clean standalone container switching blocksdocument.getElementById('panel-lookup').style.display = (tab === 'lookup') ? 'block' : 'none';document.getElementById('panel-pattern-explorer').style.display = (tab === 'lookup') ? 'block' : 'none';document.getElementById('panel-pro').style.display = (tab === 'pro') ? 'block' : 'none';document.getElementById('panel-admin').style.display = (tab === 'admin') ? 'block' : 'none';}function getBadge(val) {return val ? <span class="badge badge-yes">Yes</span> : <span class="badge badge-no">No</span>;}async function doLookup(targetNum = null) {const num = targetNum || document.getElementById('lookup-number').value.trim();const status = document.getElementById('lookup-status');const view = document.getElementById('lookup-result');if(!num) return;status.textContent = "Scanning network matrices directly...";view.style.display = 'none';try {const res = await fetch(host/lookup/{encodeURIComponent(num)});const data = await res.json();status.textContent = "Data payload synchronization secure.";view.style.display = 'block';document.getElementById('res-carrier').textContent = data.carrier.name;document.getElementById('res-line').textContent = data.carrier.line_type;document.getElementById('res-biz').textContent = data.business.name;document.getElementById('res-risk').textContent = \${data.spam.label};document.getElementById('res-yelp').innerHTML = getBadge(data.directories.yelp);document.getElementById('res-google').innerHTML = getBadge(data.directories.google_business);document.getElementById('res-court').innerHTML = getBadge(data.public_records.court_filings);document.getElementById('lbl-last-lookup').textContent = num;if(authenticatedUser) {await fetch(\${host}/pro/auto_save, {method: 'POST',headers: {'Content-Type': 'application/json'},body: JSON.stringify({ email: authenticatedUser, number: num, intelligence: data })});syncProDashboard();}} catch(e) { status.textContent = "Scan error."; }}async function doPatternSearch() {const ac = document.getElementById('pat-area-code').value;const suffix = document.getElementById('pat-suffix').value.trim();const box = document.getElementById('pattern-results');if(suffix.length !== 4) return alert("Requires a 4-digit token footprint.");box.innerHTML = "Querying internal ledger channels...";try {const res = await fetch(\${host}/pattern_search/ac/{suffix});const data = await res.json();box.innerHTML = "";data.matches.forEach(item => {const row = document.createElement('div');row.className = "data-row";row.style.cursor = "pointer";row.onclick = () => {document.getElementById('lookup-number').value = item.number;doLookup(item.number);setTab('lookup');};row.innerHTML = `${item.number}${item.carrier} · ${item.line_type}${item.business}`;box.appendChild(row);});} catch(e) { box.textContent = "Search lookup bottleneck."; }}async function proLogin() {const email = document.getElementById('pro-email').value.trim();const password = document.getElementById('pro-password').value.trim();const status = document.getElementById('pro-status');try {const res = await fetch(${host}/pro/login, {method: 'POST',headers: {'Content-Type': 'application/json'},body: JSON.stringify({ email, password })});if(!res.ok) throw new Error("Cryptographic tracking credentials rejected.");authenticatedUser = email;document.getElementById('lbl-pro-user').textContent = email;document.getElementById('pro-user-lbl').textContent = email;document.getElementById('pro-auth-block').style.display = 'none';document.getElementById('pro-session-block').style.display = 'block';status.textContent = "";syncProDashboard();} catch(e) { status.textContent = e.message; status.className = "status error"; }}function proSignOut() {authenticatedUser = null;document.getElementById('lbl-pro-user').textContent = "Not signed in";document.getElementById('pro-auth-block').style.display = 'block';document.getElementById('pro-session-block').style.display = 'none';document.getElementById('pro-ledger-view').textContent = "Login to view synchronized lookups.";}async function syncProDashboard() {const box = document.getElementById('pro-ledger-view');if(!authenticatedUser) return;const res = await fetch(${host}/pro/dashboard?email=${encodeURIComponent(authenticatedUser)});const data = await res.json();box.innerHTML = "";if(data.saved.length === 0) {box.textContent = "No history logged yet.";return;}data.saved.forEach(item => {const row = document.createElement('div');row.className = "data-row";row.innerHTML = <div style="text-align:left;"> <strong>${item.number}</strong> <div style="font-size:11px; color:#64748b;">${item.intel.carrier.name} (${item.intel.carrier.line_type})</div> </div> <span style="color:#34d399; font-size:12px;">${item.intel.business.name}</span>;box.appendChild(row);});}async function adminLoadDB() {const password = document.getElementById('admin-password').value.trim();const status = document.getElementById('admin-status');const box = document.getElementById('admin-db-view');try {const res = await fetch(${host}/admin/db?password=${encodeURIComponent(password)});if(!res.ok) throw new Error("Verification failed.");const data = await res.json();status.textContent = "Schema data map verified.";box.innerHTML = data.numbers.map(n => <div class="data-row"> <span>${n.number}</span> <span style="color:#64748b;">${n.count} scans logged</span> </div>).join("");} catch(e) { status.textContent = e.message; status.className = "status error"; }}"""@app.get("/", response_class=HTMLResponse)def index():return HTMLResponse(INDEX_HTML)def start_backend():uvicorn.run(app, host="0.0.0.0", port=8000, log_level="warning")if