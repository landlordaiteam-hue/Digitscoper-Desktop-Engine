cursor.execute("SELECT is_pro FROM users WHERE email = ?", (req.email,))row = cursor.fetchone()if not row or not row[0]:conn.close()raise HTTPException(status_code=403, detail="Pro required")now = datetime.utcnow().isoformat()try:cursor.execute("INSERT INTO saved_patterns (user_email, pattern, created_at) VALUES (?, ?, ?)",(req.email, req.pattern, now))conn.commit()except sqlite3.IntegrityError:passcursor.execute("SELECT pattern FROM saved_patterns WHERE user_email = ?", (req.email,))user_patterns = [r[0] for r in cursor.fetchall()]conn.close()return {"saved_patterns": user_patterns}@app.get("/pro/dashboard")def pro_dashboard(email: str):conn = sqlite3.connect(DB_FILE)cursor = conn.cursor()cursor.execute("SELECT is_pro FROM users WHERE email = ?", (email,))row = cursor.fetchone()if not row or not row[0]:conn.close()raise HTTPException(status_code=403, detail="Pro required")cursor.execute("SELECT number FROM saved_numbers WHERE user_email = ?", (email,))user_numbers = [r[0] for r in cursor.fetchall()]cursor.execute("SELECT pattern FROM saved_patterns WHERE user_email = ?", (email,))user_patterns = [r[0] for r in cursor.fetchall()]conn.close()return {"saved_numbers": user_numbers,"saved_patterns": user_patterns,"analytics": {"total_saved_numbers": len(user_numbers),"total_saved_patterns": len(user_patterns)}}@app.get("/admin/db")def admin_db(password: str):if password != ADMIN_PASSWORD:raise HTTPException(status_code=403, detail="Invalid admin password")conn = sqlite3.connect(DB_FILE)cursor = conn.cursor()cursor.execute("SELECT * FROM lookups")rows = cursor.fetchall()numbers_list = []for r in rows:numbers_list.append({"number": r[0], "carrier": json.loads(r[1]), "spam": json.loads(r[2]),"business": json.loads(r[3]), "directories": json.loads(r[4]),"public_records": json.loads(r[5]), "region": json.loads(r[6]),"first_seen": r[7], "last_seen": r[8], "lookup_count": r[9]})conn.close()return {"total_numbers": len(numbers_list), "numbers": numbers_list}@app.post("/admin/add_user")def admin_add_user(password: str, email: str, user_password: str, is_pro: bool = True):if password != ADMIN_PASSWORD:raise HTTPException(status_code=403, detail="Invalid admin password")now = datetime.utcnow().isoformat()secure_user_pass = hash_password(user_password)conn = sqlite3.connect(DB_FILE)cursor = conn.cursor()try:cursor.execute("""INSERT OR REPLACE INTO users (email, password, is_pro, created_at)VALUES (?, ?, ?, ?)""", (email, secure_user_pass, 1 if is_pro else 0, now))conn.commit()finally:conn.close()return {"status": "user_added", "email": email, "pro": is_pro}==============================================================================3. INTERFACE VISUAL LAYER (Embedded Dashboard UI String)==============================================================================INDEX_HTML = """Digitscoper Desktopbody {margin: 0;font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;background: radial-gradient(circle at top, #1f2933, #020617);color: #e5e7eb;display: flex;flex-direction: column;min-height: 100vh;}.app { display: flex; flex-direction: column; flex: 1; }.header {padding: 16px 24px;border-bottom: 1px solid #111827;display: flex;justify-content: space-between;align-items: center;background: rgba(15, 23, 42, 0.9);}.logo { font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: #38bdf8; }.tabs { display: flex; gap: 12px; }.tab {padding: 6px 12px;border-radius: 9999px;border: 1px solid #1f2937;cursor: pointer;font-size: 13px;color: #9ca3af;}.tab.active { background: #0ea5e9; color: #0b1120; border-color: #0ea5e9; }.main { flex: 1; display: flex; padding: 16px 24px; gap: 16px; }.panel {flex: 2;background: rgba(15, 23, 42, 0.85);border-radius: 12px;padding: 16px;border: 1px solid #1f2937;display: flex;flex-direction: column;gap: 12px;}.side {flex: 1;background: rgba(15, 23, 42, 0.85);border-radius: 12px;padding: 16px;border: 1px solid #1f2937;display: flex;flex-direction: column;gap: 12px;}.label { font-size: 12px; text-transform: uppercase; letter-spacing: 0.08em; color: #6b7280; }.input-row { display: flex; gap: 8px; align-items: center; }input[type="text"], input[type="email"], input[type="password"] {flex: 1;padding: 8px 10px;border-radius: 8px;border: 1px solid #374151;background: #020617;color: #e5e7eb;font-size: 13px;}button { padding: 8px 12px; border-radius: 8px; border: none; cursor: pointer; font-size: 13px; font-weight: 500; }.btn-primary { background: #0ea5e9; color: #0b1120; }.btn-secondary { background: #111827; color: #e5e7eb; }.section { border-top: 1px solid #111827; padding-top: 10px; margin-top: 10px; }.grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; font-size: 13px; }.item-label { color: #9ca3af; font-size: 11px; text-transform: uppercase; letter-spacing: 0.08em; }.item-value { color: #e5e7eb; font-size: 13px; }.status { font-size: 12px; color: #22c55e; min-height: 16px; }.error { color: #ef4444 !important; }.scroll { max-height: 260px; overflow-y: auto; font-size: 12px; }.pill { display: inline-block; padding: 4px 8px; border-radius: 9999px; background: #111827; color: #e5e7eb; font-size: 11px; margin: 2px; }.footer { padding: 12px 24px; text-align: center; font-size: 11px; color: #4b5563; border-top: 1px solid #111827; background: #020617; letter-spacing: 0.04em; }DIGITSCOPERLookupProAdminPhone lookupScanResultCarrierLine typeRegionTimezoneSpam scoreSpam labelBusiness listedBusiness nameDirectoriesPublic records (business)RegionCityStateTimezonePopulationPro accountLoginSave numberSaveSave patternSaveDashboardRefreshAdminLoad DBAdd Pro userAddLookup DBSession infoPro userNoneLast lookupNoneHints• Use ronald@example.com / password123 for Pro Access.• Use admin123 for Admin Operations.• Passwords are signed securely with dynamic bcrypt algorithms.©  Digitscoper Dashboard Utility. All Rights Reserved.document.getElementById('copyright-year').textContent = new Date().getFullYear();let currentTab = 'lookup';let proEmail = null;function setTab(tab) {currentTab = tab;['lookup', 'pro', 'admin'].forEach(t => {document.getElementById(tab-${t}).classList.remove('active');document.getElementById(panel-${t}).style.display = 'none';});document.getElementById(tab-${tab}).classList.add('active');document.getElementById(panel-${tab}).style.display = 'flex';}async function doLookup() {const number = document.getElementById('lookup-number').value.trim();const status = document.getElementById('lookup-status');const resultPanel = document.getElementById('lookup-result');if (!number) {status.textContent = "Enter a number to scan.";status.classList.remove('error');return;}status.textContent = "Scanning...";status.classList.remove('error');try {const res = await fetch(http://localhost:8000/lookup/${encodeURIComponent(number)});if (!res.ok) throw new Error("Lookup failed");const data = await res.json();status.textContent = "Scan complete.";resultPanel.style.display = 'block';document.getElementById('carrier-name').textContent = data.carrier.name || "Unknown";document.getElementById('carrier-line').textContent = data.carrier.line_type || "Unknown";document.getElementById('carrier-region').textContent = data.carrier.region || "Unknown";document.getElementById('carrier-tz').textContent = data.carrier.timezone || "Unknown";document.getElementById('spam-score').textContent = data.spam.score;document.getElementById('spam-label').textContent = data.spam.label || "Unknown";document.getElementById('biz-listed').textContent = data.business.listed ? "Yes" : "No";document.getElementById('biz-name').textContent = data.business.name || "None";document.getElementById('directories').textContent = JSON.stringify(data.directories, null, 2);document.getElementById('public-records').textContent = JSON.stringify(data.public_records, null, 2);document.getElementById('region-city').textContent = data.region.city || "N/A";document.getElementById('region-state').textContent = data.region.state || "N/A";document.getElementById('region-tz').textContent = data.region.timezone || "N/A";document.getElementById('region-pop').textContent = data.region.population || "N/A";document.getElementById('session-last-lookup').textContent = data.number;} catch (err) {status.textContent = "Error: " + err.message;status.classList.add('error');}}async function proLogin() {const email = document.getElementById('pro-email').value.trim();const password = document.getElementById('pro-password').value.trim();const status = document.getElementById('pro-status');if (!email || !password) {status.textContent = "Enter email and password.";status.classList.add('error');return;}status.textContent = "Logging in...";status.classList.remove('error');try {const res = await fetch('http://localhost:8000/pro/login', {method: 'POST',headers: {'Content-Type': 'application/json'},body: JSON.stringify({ email, password })});if (!res.ok) throw new Error("Login failed");const data = await res.json();if (data.pro) {status.textContent = "Pro login successful.";proEmail = email;document.getElementById('session-pro-user').textContent = email;proDashboard();} else {status.textContent = "Login ok, but not Pro.";}} catch (err) {status.textContent = "Error: " + err.message;status.classList.add('error');}}async function proSaveNumber() {const status = document.getElementById('pro-status');const number = document.getElementById('pro-save-number').value.trim();if (!proEmail) { status.textContent = "Login as Pro first."; status.classList.add('error'); return; }if (!number) { status.textContent = "Enter a number to save."; status.classList.add('error'); return; }status.textContent = "Saving number...";status.classList.remove('error');try {const res = await fetch('http://localhost:8000/pro/save_number', {method: 'POST',headers: {'Content-Type': 'application/json'},body: JSON.stringify({ email: proEmail, number })});if (!res.ok) throw new Error("Save failed");status.textContent = "Number saved.";proDashboard();} catch (err) {status.textContent = "Error: " + err.message; status.classList.add('error');}}async function proSavePattern() {const pattern = document.getElementById('pro-save-pattern').value.trim();const status = document.getElementById('pro-status');if (!proEmail) { status.textContent = "Login as Pro first."; status.classList.add('error'); return; }if (!pattern) { status.textContent = "Enter a pattern to save."; status.classList.add('error'); return; }status.textContent = "Saving pattern...";status.classList.remove('error');try {const res = await fetch("http://localhost:8000/pro/save_pattern", {method: "POST",headers: { "Content-Type": "application/json" },body: JSON.stringify({ email: proEmail, pattern })});if (!res.ok) throw new Error("Save failed");status.textContent = "Pattern saved.";proDashboard();} catch (err) {status.textContent = "Error: " + err.message; status.classList.add('error');}}async function proDashboard() {const status = document.getElementById('pro-status');if (!proEmail) return;try {const res = await fetch(http://localhost:8000/pro/dashboard?email=${encodeURIComponent(proEmail)});if (!res.ok) throw new Error("Dashboard failed");const data = await res.json();let html = "";html += "Saved numbers";html += data.saved_numbers.map(n => <span class='pill'>${n}</span>).join(" ");html += "Saved patterns";html += data.saved_patterns.map(p => <span class='pill'>${p}</span>).join(" ");html += "Analytics";html += <div>Total numbers: ${data.analytics.total_saved_numbers}</div>;html += <div>Total patterns: ${data.analytics.total_saved_patterns}</div>;document.getElementById('pro-dashboard').innerHTML = html;} catch (err) {status.textContent = "Error: " + err.message; status.classList.add('error');}}async function adminLoadDB() {const password = document.getElementById('admin-password').value.trim();const status = document.getElementById('admin-status');const dbDiv = document.getElementById('admin-db');if (!password) { status.textContent = "Enter admin password."; status.classList.add('error'); return; }status.textContent = "Loading DB...";status.classList.remove('error');try {const res = await fetch(http://localhost:8000/admin/db?password=${encodeURIComponent(password)});if (!res.ok) throw new Error("Admin DB failed");const data = await res.json();status.textContent = Loaded ${data.total_numbers} numbers.;dbDiv.innerHTML = "";data.numbers.forEach(rec => {dbDiv.innerHTML += <div>• ${rec.number} (lookups: ${rec.lookup_count})</div>;});} catch (err) {status.textContent = "Error: " + err.message; status.classList.add('error');}}async function adminAddUser() {const password = document.getElementById('admin-password').value.trim();const email = document.getElementById('admin-new-email').value.trim();const userPassword = document.getElementById('admin-new-password').value.trim();const status = document.getElementById('admin-status');if (!password || !email || !userPassword) {status.textContent = "Fill all admin fields.";status.classList.add('error');return;}status.textContent = "Adding user...";status.classList.remove('error');try {const res = await fetch(http://localhost:8000/admin/add_user?password=${encodeURIComponent(password)}&email=${encodeURIComponent(email)}&user_password=${encodeURIComponent(userPassword)}, {method: 'POST'});if (!res.ok) throw new Error("Add user failed");const data = await res.json();status.textContent = User added: ${data.email};adminLoadDB();} catch (err) {status.textContent = "Error: " + err.message; status.classList.add('error');}}"""@app.get("/", response_class=HTMLResponse)def index():return HTMLResponse(INDEX_HTML)==============================================================================4. APPLICATION WINDOW EXECUTION WRAPPER (PyWebView Infrastructure)==============================================================================def start_backend():uvicorn.run(app, host="127.0.0.1", port=8000, log_level="warning")def start_app():# Execute the backend API pipeline concurrently inside a sub-thread wrapper contextt = threading.Thread(target=start_backend, daemon=True)t.start()# Render native container frame layers directly mapping back to localized nodeswebview.create_window("Digitscoper Desktop", "127.0.0", width=1100, height=700)webview.start()if name == "main":start_app()